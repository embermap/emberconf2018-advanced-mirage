<div class="flex">
  <div class="w-col flex-no-shrink min-h-screen border-r border-grey-lighter px-4 pt-4">
    <h1 class='mt-3 mb-8'>Exercise 3</h1>
    <p class='text-sm leading-normal'>Write a declarative filterable handler for all 4 fields.</p>

    <div class="mt-8 mb-8">
      <p class='text-sm text-grey uppercase mb-4'>Search</p>

      {{input class='p-2 text-sm border'
        value=season
        input=(action 'search')
        type="text"
        placeholder='Season'}}

      {{input class='p-2 text-sm border mt-2'
        value=number
        input=(action 'search')
        type="text"
        placeholder='Episode number'}}

      {{input class='p-2 text-sm border mt-2'
        value=name
        input=(action 'search')
        type="text"
        placeholder='Name'}}

      {{input class='p-2 text-sm border mt-2'
        value=summary
        input=(action 'search')
        type="text"
        placeholder='Summaries'}}

      {{#if findEpisodes.isRunning}}
        <p class='mt-4'>{{fa-icon 'spinner' spin=true}} Loading...</p>
      {{/if}}
    </div>
  </div>

  <div class="flex-grow h-screen overflow-y-scroll">
    <div class="p-6">
      <div class="mx-auto">
        {{episode-list episodes=findEpisodes.lastSuccessful.value}}
      </div>
    </div>
  </div>
</div>
